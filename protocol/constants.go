package protocol

const (
	// bufferSize is the size of the buffer used to read and write data to/from the WebSocket connection.
	bufferSize = 4096
	// closeCode is the close code used to indicate a normal closure.
	closeCode = 1000
)
